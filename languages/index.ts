export default interface Language {
  pages: {
    about: string
    schedules: string
    directions: string
    sponsors: string
    staffs: string
    blog: string
  }

  register: string

  sponsors: {
    sortBy: string
  }
}
